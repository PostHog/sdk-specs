# posthog-node — SDK Compliance

**Repo:** [PostHog/posthog-js](https://github.com/PostHog/posthog-js) (`packages/node` + `packages/core` — the standalone `posthog-node` repo is archived and its code now lives in this monorepo)
**Audited commit:** `fbdb6c7b8ffdc523b19b3ea82f8ebcfa8fb065b5` ([commit](https://github.com/PostHog/posthog-js/commit/fbdb6c7b8ffdc523b19b3ea82f8ebcfa8fb065b5)) — audited on 2026-08-17
**Audited against sdk-specs commit:** `0ea0aba45170a56a8778197a3e3bd9c6e9b3dd79`
**Summary:** 10 ✅ · 16 🟡 · 8 ❌ · 28 ➖ · 0 ❓

**A note on repo layout:** The `PostHog/posthog-node` repository itself has been archived/redirected — its code was moved into the `posthog-js` monorepo and now lives at `packages/node` (Node-specific integration: the `PostHog` client class, Express/NestJS middleware, error-tracking stack-trace enrichment, feature-flag polling, the new `captureAi`/`ai-capture` route) and `packages/core` (the shared `@posthog/core` library also used by `packages/browser` and `packages/react-native`: the capture/batch/flush/retry engine in `posthog-core-stateless.ts`, the richer ambient-identity `posthog-core.ts` base class used only by client SDKs, logs, error-tracking primitives). This audit clones `posthog-js` (commit above) and inspects `packages/node/src` + `packages/core/src` only — `packages/browser`, `packages/react-native`, and `packages/ai` are out of scope except where they are the *only* place a Node-facing option (e.g. `enableFullAiCapture`) is actually consumed. `packages/node`'s `PostHogBackendClient` still extends the stateless `PostHogCoreStateless`, **not** the richer client-only `PostHogCore`, so most client-only ambient-identity/session/replay primitives (`group()`, `reset()`, `getAnonymousId()`, `onFeatureFlags()`, session replay, bootstrap, etc.) simply don't exist on the Node class at all, making most N/A calls unusually clean-cut. The repo still supports both a legacy "v0" batch-capture pipeline (`POST /batch/`, default) and a newer "v1" pipeline (`POST /i/v1/analytics/events`, opt-in only via `POSTHOG_CAPTURE_MODE=v1` env var, `packages/node/src/capture-v1/config.ts`) — both share one queue/flush/retry engine in `posthog-core-stateless.ts` but differ in transport (`packages/node/src/capture-v1/*`), which matters for several findings below. Since the last audit (2026-08-06, `e1efa572`), the monorepo has added a dedicated AI-event capture route (`packages/node/src/ai-capture/`), a richer flag-evaluation snapshot API (`packages/node/src/feature-flag-evaluations.ts`), and no Traces/OTel-native-spans support in `packages/node` (still only in the separate `packages/ai` package's OTel bridge to `/i/v0/ai/otel`, not `/i/v1/traces`).

| # | Contract | Status | Note |
|---|----------|--------|------|
| 1 | Logs | 🟡 | [n1] |
| 2 | Traces | ❌ | [n2] |
| 3 | Tracing Headers | 🟡 | [n3] |
| 4 | Alias | 🟡 | [n4] |
| 5 | Capture | 🟡 | [n5] |
| 6 | Capture AI | 🟡 | [n6] |
| 7 | Capture Exception | ❌ | [n7] |
| 8 | Create Person Profile | ➖ | client-side identity/profile-control API (`Applicability: client`); `createPersonProfile()` exists only on the client-only `PostHogCore` base class (`packages/core/src/posthog-core.ts:1508`), never on `PostHogCoreStateless`/`packages/node`; acceptance file is `@client`-only |
| 9 | Debug | ✅ | [n8] |
| 10 | Exception Steps | ➖ | spec applicability explicitly scopes the single-user buffer to client SDKs, with the most explicit server-exclusion language of any spec ("server/multi-tenant explicitly out of scope pending a follow-up spec"); acceptance is `@client` on every scenario; `ExceptionStepsBuffer` is consumed only by `packages/browser`/`packages/react-native`, zero references in `packages/node/src` |
| 11 | Flush | 🟡 | [n9] |
| 12 | Get Anonymous ID | ➖ | client-side ambient-identity getter (`Applicability: client`); `getAnonymousId()` exists only on `PostHogCore` (`posthog-core.ts:307`), unreachable from node; acceptance is `@client`-only |
| 13 | Get Distinct ID | ➖ | client-side ambient-identity getter (`Applicability: client`); no `getDistinctId()` on the Node client — every call takes an explicit `distinctId` argument. Note: acceptance file is now `@both` at the feature level with an explicit `@server` scenario ("Server SDKs do not expose ambient distinct id state") requiring server SDKs to positively assert the absence of this getter — Node satisfies this by omission, so the N/A verdict still holds, but the justification basis shifted from "no server scenario exists" to "a server scenario exists and is satisfied by not implementing the method" |
| 14 | Get Feature Flag | ✅ | |
| 15 | Get Feature Flag Payload | ✅ | |
| 16 | Get Feature Flag Result | ✅ | |
| 17 | Get Feature Flags | ✅ | |
| 18 | Get Feature Flags And Payloads | ✅ | |
| 19 | Evaluate Flags | ✅ | [n10] |
| 20 | Get Session ID | ➖ | client-side ambient session-manager read (`Applicability: client`); node's only "session id" concept is a caller-supplied `AsyncLocalStorage` correlation value for stamping outgoing events/headers, not an ambient create/rotate/persist manager; acceptance is `@client`-only |
| 21 | Group | ➖ | client-side ambient-context setter (`Applicability: client`); no method named `group()` exists anywhere in `packages/node/src` — node exposes `groupIdentify()`/`groupIdentifyImmediate()` instead, matching the spec's own stated server-SDK alternative; acceptance is `@client`-only |
| 22 | Group Identify | 🟡 | [n11] |
| 23 | Identify | 🟡 | [n12] |
| 24 | Is Feature Enabled | ❌ | [n13] |
| 25 | Is Opt Out | ➖ | client-side persisted consent-state getter (`Applicability: client`); acceptance is `@client`-only; node has an internal `optedOut` getter (`packages/core/src/posthog-core-stateless.ts:402`) but never exposes an `isOptedOut()`/`isOptOut()`-named public method |
| 26 | Is Session Replay Active | ➖ | client-side session-replay status getter (`Applicability: client`); zero matches for `isSessionReplayActive` anywhere in `packages/node`/`packages/core`; acceptance is `@client`-only |
| 27 | On Feature Flags | ➖ | client-side callback/event surface tied to ambient flag state (`Applicability: client`); `onFeatureFlags()` exists only on `PostHogCore`, unreachable from node; acceptance is `@client`-only |
| 28 | Opt In | ➖ | client-side persistent per-user consent toggle (`Applicability: client`); acceptance is `@client`-only; node has an internal `optIn()`/`optOut()` pair on `PostHogCoreStateless` but the Node client publicly renames these `enable()`/`disable()` (`client.ts:664,682`), not the canonical `optIn`/`optOut` contract |
| 29 | Register | 🟡 | [n14] |
| 30 | Reload Feature Flags | ➖ | client-side ambient per-identity flag-cache refresh primitive (`Applicability: client`); acceptance is `@client`-only; node's same-named `reloadFeatureFlags()` (`client.ts:2363`) is a structurally different primitive — refreshes the global local-evaluation rule-definitions cache, not an ambient per-identity flag-value cache |
| 31 | Reset | ➖ | client-side ambient-identity/session/super-properties clear, including the spec's new bootstrap-options reset extension (`Applicability: client`); zero `bootstrap` references anywhere in `packages/node/src`; the only `reset(` symbol in node is an unrelated internal AI-metrics counter reset (`client.ts:2589`, `this._metrics.reset()`); acceptance is `@client`-only |
| 32 | Reset Group Properties For Flags | ❌ | [n15] |
| 33 | Reset Person Properties For Flags | ❌ | [n15] |
| 34 | Screen | ➖ | client-side screen-view capture primitive (`Applicability: client`); `screen(` exists only in `packages/react-native`, zero hits in `packages/node`/`packages/core`; acceptance is `@client`-only |
| 35 | Set Group Properties For Flags | ❌ | [n15] |
| 36 | Set Person Properties | ✅ | [n16] |
| 37 | Set Person Properties For Flags | ❌ | [n15] |
| 38 | Setup | ➖ | client-side initialization surface (`Applicability: client`); Node's `PostHogBackendClient` is a synchronous one-shot constructor with no singleton registry, no async readiness gap, and no persisted local state to restore; acceptance is `@client`-only |
| 39 | Shutdown | 🟡 | [n17] |
| 40 | Start Session Recording | ➖ | client-side session-replay control (`Applicability: client`); acceptance is `@client`-only |
| 41 | Stop Session Recording | ➖ | client-side session-replay control (`Applicability: client`); acceptance is `@client`-only |
| 42 | Unregister | 🟡 | [n14] |
| 43 | Application Lifecycle | ➖ | mobile/native app install/foreground/background lifecycle; spec explicitly excludes server SDKs; acceptance is `@client`-only |
| 44 | Autocapture | ➖ | browser/mobile UI-event auto-capture; spec explicitly excludes server SDKs ("do not own a UI event stream"); node's own `extensions/error-tracking/autocapture.ts` is a same-named but unrelated concept (`uncaughtException`/`unhandledRejection` process hooks for exception capture, not UI events); acceptance is `@client`-only |
| 45 | Before Send Hook | ✅ | [n18] |
| 46 | Consent Gating | ➖ | client-side persistent per-user consent gate (`Applicability: client`); acceptance is `@client`-only; node's in-memory-only `optIn`/`optOut`/`optedOut` mechanism is a coarser, non-durable, process-wide gate distinct from the persisted per-user gate the spec describes |
| 47 | Device ID Generator | ➖ | client-side persisted anonymous/device-id bootstrap (`Applicability: client`, React-Native-specific); node never generates/persists a device id, only accepts one as an optional caller-supplied bucketing property; acceptance is `@client`-only |
| 48 | Event Batcher | ✅ | [n19] |
| 49 | Exception Event Metadata | ❌ | [n20] |
| 50 | Feature Flag Cache | ➖ | spec's own `Applicability` is `client`-only ("the audited implementations here are the ambient caches used by client SDKs"); node has no evaluated-flag-*value* cache under any name (only a flag-*definitions* cache, a different contract — see Flag Definition Loader), which is consistent with this contract simply not applying to a server SDK rather than a partial implementation |
| 51 | Feature Flag Called Tracker | 🟡 | [n21] |
| 52 | Flag Definition Loader | 🟡 | [n22] |
| 53 | HTTP Client | 🟡 | [n23] |
| 54 | Local Feature Flag Evaluator | 🟡 | [n24] |
| 55 | Persistent Storage | ➖ | client-side persistent state layer (ambient identity/cached SDK state) (`Applicability: client`); node's only storage is `PostHogMemoryStorage`, a pure in-process object with zero disk/fs I/O anywhere in `packages/node`/`packages/core`; acceptance is `@client`-only |
| 56 | Remote Config | ➖ | client-side ambient project-config bootstrap (session replay/surveys/flags config) (`Applicability` context implies client); node's `getRemoteConfigPayload()` (`client.ts:1650`) is a narrow, personal-API-key-gated, per-flag encrypted/JSON payload fetch, structurally unrelated to the broader session-replay/surveys/error-tracking config bootstrap the spec describes, none of which exists in `packages/node` |
| 57 | Retry Queue | 🟡 | [n25] |
| 58 | Session Manager | ➖ | client-side ambient session id/rotation/persistence (`Applicability: client`); real session rotation/timeout logic exists only on `PostHogCore`, unreachable from node; acceptance is `@client`-only |
| 59 | Session Replay Ingestion Controls | ➖ | browser/mobile session-replay sampling/trigger controls (`Applicability: client`); acceptance is `@client`-only |
| 60 | Session Replay Privacy | ➖ | browser/mobile DOM/screenshot masking (`Applicability: client`); no masking primitive of any kind exists in `packages/node`; acceptance is `@client`-only |
| 61 | Surveys | ➖ | browser/mobile survey display lifecycle, including the spec's new intro-screen requirement (UI-only); `packages/core/src/surveys/` exists as validation helpers but has zero references anywhere in `packages/node/src`; acceptance is `@client`-only |
| 62 | Bootstrap | ➖ | client-side cold-start flag/identity hydration from server-rendered values; `setupBootstrap()` exists only on `PostHogCore`, called only by `posthog-react-native`, zero node references; acceptance is `@client`-only |

## Notes

### n1 — Logs (🟡 Partial)
- **Spec requires:** A `captureLog`/logger API producing OTLP-shaped log records shipped via `POST {host}/i/v1/logs`, with severity-number mapping, batching/rate-capping, gzip, and retry/413 handling.
- **SDK currently:** A complete, spec-conformant logs pipeline exists in shared `@posthog/core` — `packages/core/src/logs/index.ts` (`PostHogLogs.captureLog()`, tumbling-window rate cap, FIFO oldest-eviction overflow, persisted queue, exponential-backoff retry, 413 batch-halving) and `packages/core/src/logs/logs-utils.ts` (OTLP severity/value mapping). The transport, `PostHogCoreStateless._sendLogsBatch()`, correctly POSTs to `${host}/i/v1/logs?token=...` and gzips. **None of this is wired into `packages/node`.** A fresh grep confirms `grep -rn "captureLog\|PostHogLogs\|LogsConfig\|logsHost" packages/node/src` returns zero matches, and `packages/node/src/exports.ts:15` re-exports only `Metrics`/`MetricsConfig` from `@posthog/core`, nothing from `logs`. There is still no `public get logger()` getter on `PostHogBackendClient` analogous to the existing `public get metrics()` getter (`client.ts:598`) that wires up the sibling `PostHogMetrics` class. A Node application cannot call `captureLog` today even though the transport layer is present on every Node client instance.
- **Backwards compatibility:** Backward-compatible — instantiating `PostHogLogs` and exposing it via a new lazy getter/method on `PostHogBackendClient`, mirroring the existing `metrics` getter pattern, is purely additive.
- **Remediation:** Add a `captureLog()` method or `logger`/`logs` getter to `packages/node/src/client.ts` that constructs `PostHogLogs` against the already-present `_sendLogsBatch` transport, the same way `metrics` is wired up today.

### n2 — Traces (❌ Fail)
- **Spec requires:** A `startSpan`/`withSpan` API producing OTLP spans shipped to `POST {host}/i/v1/traces`, explicitly distinct from LLM-analytics-as-events, requiring no OpenTelemetry runtime dependency.
- **SDK currently:** Exhaustive grep for `startSpan`/`withSpan`/`i/v1/traces`/`traceparent`/`getActiveSpan` across `packages/core/src`, `packages/browser/src`, `packages/react-native/src`, and `packages/node/src` returns zero matches anywhere in the monorepo. What exists instead is exactly the substitute the spec explicitly excludes: LLM analytics captured as `$ai_generation`/`$ai_trace_id` ordinary analytics events, plus `packages/ai/src/otel/{exporter,processor}.ts`, which posts OTel spans to `${host}/i/v0/ai/otel` (not `/i/v1/traces`), only for `gen_ai.*` AI-semantic-convention spans, and depends on real `@opentelemetry/sdk-trace-base`/`@opentelemetry/exporter-trace-otlp-http` packages — the exact OTel runtime dependency the spec says the canonical implementation must avoid. This bridge lives in the separate `packages/ai` package, which `packages/node`'s `package.json` does not depend on. No SDK in this monorepo implements the canonical contract yet — this is a monorepo-wide gap, not a node-specific regression, and is unchanged since the last audit.
- **Backwards compatibility:** Backward-compatible — a net-new subsystem; no removal needed.
- **Remediation:** Implement a dependency-free `startSpan`/`withSpan` API shipping OTLP spans to `/i/v1/traces` in `@posthog/core`, wired into `packages/node` (and browser/react-native), independent of the `packages/ai` OTel bridge.

### n3 — Tracing Headers (🟡 Partial)
- **Spec requires:** `both` applicability. Request-scoped middleware extracting `X-POSTHOG-DISTINCT-ID`/`X-POSTHOG-SESSION-ID` case-insensitively; sanitize (trim/strip control chars/cap length); explicit call-site values take precedence over context; personless-capture fallback unless the caller explicitly supplied `$process_person_profile`; flag-evaluation APIs no-op/return-empty when no identity is resolvable; per-request isolation; headers never used for auth.
- **SDK currently:** Server-side consumption is real and well-tested. `packages/node/src/extensions/context/context.ts` implements genuine `AsyncLocalStorage`-based per-request isolation. `packages/node/src/extensions/tracing-headers.ts` reads headers case-insensitively (Node's `IncomingHttpHeaders` is always lowercased); `sanitizeTracingHeaderValue` strips control chars, trims, caps at 1000 chars. Two framework integrations wire this in: `express.ts` and `nestjs.ts`, both never swallowing errors. Capture precedence is correct in `_prepareEventMessage` (`client.ts:~2920-2940`): explicit `distinctId` wins over context, explicit properties win over context properties. Personless fallback (`client.ts:2934-2937`, synthesize `uuidv7()` + `$process_person_profile=false`) remains **unconditional**, executed after the property spread, so it overwrites any caller-supplied `$process_person_profile` value rather than only applying "unless the caller explicitly supplied that property" — a real, narrow, unchanged deviation. Flag-evaluation no-op-when-no-identity is correctly implemented on the modern API surface (`getAllFlags`, `getAllFlagsAndPayloads`, `evaluateFlags`, `getFeatureFlagResult` all resolve via a shared distinct-id-resolution helper and return early with no network call and no `$feature_flag_called` when unresolvable). Gap: the deprecated-but-still-shipped legacy API surface (`getFeatureFlag`, `isFeatureEnabled`) still requires `distinctId` as a mandatory positional argument with no context-fallback and no no-op path.
- **Backwards compatibility:** Backward-compatible — adding an options-overload with context-fallback to the legacy `getFeatureFlag`/`isFeatureEnabled` methods, and fixing the personless-fallback to respect a caller-supplied `$process_person_profile`, are both additive/narrowing fixes.
- **Remediation:** Add optional-`distinctId`-with-context-fallback support to `getFeatureFlag`/`isFeatureEnabled`; guard the personless-fallback assignment in `_prepareEventMessage` with a check for an already-present `$process_person_profile` property.

### n4 — Alias (🟡 Partial)
- **Spec requires:** `both` applicability. Validate inputs — both the existing distinct id and the alias target id must be present; if either is missing, drop the call with zero enqueued events and a validation warning (acceptance scenario "Alias is dropped when required identities are missing," tagged `@both`).
- **SDK currently:** `alias()`/`aliasImmediate()` (`packages/node/src/client.ts:~1050-1090`) build the correct `$create_alias` event shape for the happy path. There is still no validation of `data.distinctId`/`data.alias` for emptiness anywhere in the method (confirmed by fresh code read — no guard clause exists before `_sendPreparedEvent`). Both flow through the shared `_prepareEventMessage`, where a falsy `distinctId` is silently replaced with a synthesized `uuidv7()` and the event is enqueued anyway with `$process_person_profile: false` — rather than being dropped with a validation warning as the acceptance scenario requires. Unchanged from the previous audit.
- **Backwards compatibility:** Backward-compatible — adding an emptiness check that logs a warning and returns early is additive; it only changes behavior for a currently-malformed call that today silently mutates the distinct id instead of failing loudly.
- **Remediation:** In `alias()`/`aliasImmediate()`, validate that `data.distinctId` and `data.alias` are both present and non-empty before building the event; log a warning and return without enqueuing otherwise.

### n5 — Capture (🟡 Partial)
- **Spec requires:** `both` applicability. Enrich events with `distinct_id`/timestamp/uuid/`$lib`+`$lib_version`/super-properties/`$groups`; honor an opt-out state so capture is silently dropped with no network call; validate that `event` and `distinct_id` are non-empty; support a `before_send` hook that can mutate/drop events, with hook exceptions logged and the original message used as a fallback (not the event being dropped).
- **SDK currently:** `capture()` is fire-and-forget and never throws. Enrichment is complete and correct: `$lib`/`$lib_version`/`$is_server`, timestamp/uuid defaulting, `$groups`, `$process_person_profile=false` fallback for missing distinct id. A genuine dynamic opt-out exists (`optIn()`/`optOut()`, exposed as `enable()`/`disable()`), checked in `enqueue()`/`sendImmediate()` with silent no-op. Gaps, both reconfirmed unchanged against current code: (1) no runtime validation of `event`/`distinct_id` non-emptiness anywhere — `_warnIfInvalidCapture` (`client.ts:710-721`) only checks for string-typed `properties` or `$exception` misuse, not empty event/distinctId; a falsy `distinctId` is silently replaced with a synthesized UUID instead of triggering a validation warning; (2) `before_send` hook exceptions are not caught at the Node-specific hook-invocation site `_runBeforeSend` (`client.ts:3050-3072`, distinct from the try/catch-wrapped version in `PostHogCore` used only by browser/react-native) — an uncaught exception propagates as a rejected promise and is only caught one layer above by a generic `.catch` in `_sendPreparedEvent` (`client.ts:750-754`), which drops the entire event via `console.error` rather than falling back to the original, unmutated message as the spec requires.
- **Backwards compatibility:** Backward-compatible — adding event/distinct_id validation and fixing the before_send exception path to preserve the original message (rather than dropping) are both additive/bug-fix-shaped changes with no public signature impact.
- **Remediation:** Add non-empty validation for `event` and `distinct_id` with a logged warning on failure; wrap the Node-specific `_runBeforeSend`'s hook invocation in its own try/catch that falls back to the pre-hook message on exception, matching `PostHogCore`'s existing browser/RN behavior.

### n6 — Capture AI (🟡 Partial)
- **Spec requires:** `server` applicability. `captureAi`/`captureAiImmediate` mirroring `capture`'s shape on a fully isolated AI-event queue/route/endpoint, never sharing a send cycle with `capture()` even for `$ai_*`-named events; UUID generation/validation identical to `capture`; the route never throws; oversized events are dropped while logging only event name + byte size, never content; `enableFullAiCapture` gates wrapper-level behavior (media passthrough, skip truncation) without altering `capture_ai` payloads directly; **privacy mode always wins over `enableFullAiCapture`**, regardless of client-level or per-call configuration.
- **SDK currently:** This is a mature, well-tested, near-complete implementation. `captureAi`/`captureAiImmediate` (`packages/node/src/client.ts:853-887`) mirror `capture`'s `EventMessage` shape exactly (`types.ts:447,459`). The route is genuinely isolated: dedicated `AI_CAPTURE_ROUTE` (`ai-capture/routing.ts:1`), its own persisted queue key, its own endpoint `/i/v0/ai/batch/`, and independent flush handling — verified never to mix with the ordinary analytics route even when `capture()` is called with an `$ai_*`-named event (`__tests__/ai-capture/wiring.spec.ts:58-79`). UUID generation/validation reuses the shared `getEventUuid`/`isValidUUID` helpers. The route never throws (`_sendPreparedAiEvent` funnels through the same `.catch((err) => console.error(err))` guard as ordinary capture). Oversized-event dropping is correctly implemented: `partitionAiBatch` (`ai-capture/batching.ts:16-47`) computes byte size and for events over the 8MiB cap (`ai-capture/routing.ts:5`) logs/emits only `` `Event ${event} (${bytes} bytes) exceeds the 8MiB limit...` `` — name and size only, confirmed never containing payload content, and directly tested (`wiring.spec.ts:87-109`). `enableFullAiCapture` (`types.ts:346`, `client.ts:151,219`) is correctly scoped to the separate `packages/ai` wrapper package (media redaction, truncation skip) rather than altering `captureAi` payloads directly, matching the spec's "capture_ai is a pipe" requirement. **Gap: the "privacy mode always wins" guarantee is broken at the client-config level.** `packages/ai/src/utils.ts:384-386`'s `withPrivacyMode` correctly ORs a client-level `privacy_mode` with a per-call `privacyMode` argument and redacts if either is true — but `PostHogOptions.privacyMode` (`packages/node/src/types.ts:194`, documented again at `types.ts:341-343` as "Privacy mode always wins") is **never read, stored, or assigned anywhere** in `packages/node/src/client.ts` or `packages/core/src` — no constructor assignment, no getter. `packages/ai`'s own tests only exercise this path by manually monkey-patching a mock client (`(mockPostHogClient as any).privacy_mode = true`, e.g. `packages/ai/tests/anthropic.test.ts:753`), which a real `new PostHog(key, { privacyMode: true })` client can never reproduce. The per-call `privacyMode` argument passed directly to an AI-wrapper call does work correctly and unconditionally redacts — only the persistent client-level override is dead.
- **Backwards compatibility:** Backward-compatible — this is a strengthening fix (the spec explicitly permits guarantees to strengthen, never weaken, pre-GA); no existing caller currently gets any redaction benefit from setting `privacyMode` at the client level today since it does nothing, so wiring it up can only add behavior for callers who already believed they'd set it.
- **Remediation:** In `packages/node/src/client.ts`, store the normalized `privacyMode` option as a real property/getter (e.g. `public readonly privacyMode: boolean`, or a `privacy_mode` getter matching the snake_case name `packages/ai`'s `withPrivacyMode` already reads) so a real client instance carries the value the type declares; add a regression test asserting that constructing a client with `{ privacyMode: true, enableFullAiCapture: true }` and calling a `packages/ai` wrapper redacts input/output.

### n7 — Capture Exception (❌ Fail)
- **Spec requires (acceptance-tag precedence, `@both`):** Correctly-ordered `$exception_list`/stack frames, flat `$exception_type`/`$exception_message`, and — per the newly-added, much deeper Exception Event Metadata contract that this row now overlaps with — a `mechanism` object carrying `exception_id`/`parent_id` for tree linkage, independently-tracked handled/synthetic state per nested exception (never derived from the outer exception), a canonical `$exception_source`, correct `$exception_level` normalization (including `fatal` for terminating boundaries), and SDK-owned properties that cannot be overridden by caller-supplied property bags.
- **SDK currently:** Ordering/stack-parsing/source-context fundamentals remain correct (`packages/core/src/error-tracking/error-properties-builder.ts`, `packages/core/src/error-tracking/parsers/index.ts`, `packages/node/src/extensions/error-tracking/modifiers/context-lines.node.ts`). However, deep verification against the new Exception Event Metadata spec (see n20) surfaced multiple hard failures that also break this contract directly: (1) `$exception_type`/`$exception_message` flat properties are still never added on the standard `captureException()` path (`buildFromUnknown()`, `error-properties-builder.ts:29-44`, returns only `$exception_list`/`$exception_level`) — these exist only in the unrelated Sentry-adapter code path; (2) nested `.cause` entries are hardcoded to `handled: true` regardless of their true state (`convertToExceptionList`, `error-properties-builder.ts:119-126`, comment "Cause errors are necessarily handled"), directly contradicting the requirement that each nested entry preserve its own independently-known handled state; (3) `$exception_level` is hardcoded to the literal `'error'` for every capture path including uncaught-exception boundaries that actually terminate the process (`error-properties-builder.ts:42`; Node's default `uncaughtException` handling does call `process.exit(1)` per `autocapture.ts:106-110`, so this is a live, reachable inaccuracy, not theoretical); (4) caller-supplied `additionalProperties` are spread *after* (and therefore override) the SDK-canonical `$exception_list`/`$exception_level` properties in `ErrorTracking.buildEventMessage` (`packages/node/src/extensions/error-tracking/index.ts:52,67-69`), letting application code silently corrupt exception metadata. Given the severity and number of these overlapping failures (detailed fully under n20), this row is downgraded from the previous audit's 🟡 Partial to ❌ Fail.
- **Backwards compatibility:** Backward-compatible for (1) flat-property addition and (4) reserved-key protection (both additive/hardening changes); Needs deprecation path for (2) and (3), since fixing the hardcoded `handled`/`$exception_level` values changes currently-shipped event payload shapes that downstream consumers (dashboards, alerts) may already depend on in their current (incorrect) form.
- **Remediation:** See n20 for the full remediation list — add flat `$exception_type`/`$exception_message`; thread real per-cause `handled` state instead of hardcoding `true`; derive `$exception_level` from mechanism/fatality context instead of a hardcoded literal; reserve `$exception_*` keys against caller-supplied property-bag overrides in `ErrorTracking.buildEventMessage`.

### n8 — Debug (✅ Pass)
- Spec's own applicability line says `client`, and the acceptance file (`acceptance/public/debug.feature`) is tagged `@client` throughout with no scenario-level override. However, posthog-node genuinely implements the exact behavior the spec describes: `debug(enabled: boolean = true): void` is explicitly overridden at `packages/node/src/client.ts` (calls `super.debug(enabled)` then forwards to the feature-flag poller), building on `PostHogCoreStateless.debug()` (installs/removes a wildcard `on('*', ...)` internal-event logger with no capture/network side effects). This mechanically satisfies all 3 acceptance scenarios. Recorded as ✅ (implemented beyond the spec's own stated `client` scope) rather than ➖, since the contract genuinely exists and works.

### n9 — Flush (🟡 Partial)
- **Spec requires:** `@both`. No-op on empty queue with no network call; bypass batching intervals and drain immediately; on delivery failure the failed batch must remain queued for a later retry, not silently dropped.
- **SDK currently:** `flush()` is correctly a no-op on an empty queue and drains the entire queue across as many sequential batches as needed (`_flushRoute`, `posthog-core-stateless.ts:1548-1632`). On the **v0** pipeline (still the default — `resolveCaptureMode()`, `capture-v1/config.ts:15-18`, defaults to `'v0'`, with v1 reachable only via the undocumented `POSTHOG_CAPTURE_MODE` env var), a retryable failure correctly skips `persistQueueChange()` so events remain queued — matches spec. **On the v1 pipeline, this guarantee is still broken**: `V1CaptureSender.sendV1Batch` (`packages/node/src/capture-v1/sender.ts:93-186`) never throws for a handled outcome — even after exhausting its retry budget it calls `onError()` and resolves normally (the class's own docstring at lines 68-70 confirms this is deliberate: "resolves, so the caller's queue treats the batch as consumed"). Because `_flushRoute`'s `persistQueueChange()` runs unconditionally at line 1627 for any non-throwing path, it evicts the batch from the queue as if delivered even when delivery ultimately failed. This is currently latent in practice since v1 is opt-in-only, but it is a real, unfixed, reproducible bug against "Flush keeps events retryable when delivery fails" for any v1-mode client.
- **Backwards compatibility:** Needs deprecation path — fixing v1 to re-throw on exhausted retry (so `_flushRoute` preserves the queue) changes v1's currently-documented "never throws" error-surfacing contract, which downstream code/tests may depend on; should be rolled out carefully, not as a silent behavior change.
- **Remediation:** Make `V1CaptureSender.sendV1Batch` propagate a retryable failure signal (e.g. throw or return a discriminated failure result) when its own retry budget is exhausted, so `_flushRoute` can correctly preserve the batch in the queue instead of treating it as delivered.

### n10 — Evaluate Flags (✅ Pass)
- **Spec requires:** `server` applicability. Snapshot semantics (accessor reads never re-evaluate or hit the network); remote-fallback-fills-once (a single remote call fills only the gaps local evaluation couldn't decide, never overwriting locally-resolved values); point-in-time immutability; silent payload access (no `$feature_flag_called` on payload reads); lazy access tracking with dedupe via the same feature-flag-called tracker used elsewhere; capture-enrichment reuse (`capture(..., flags: snapshot)` taking precedence over the deprecated `sendFeatureFlags` option); in-memory `only`/`onlyAccessed` filtering; safe empty/partial snapshots on missing identity or evaluation failure.
- **SDK currently:** A mature, thorough implementation in `packages/node/src/feature-flag-evaluations.ts` (`FeatureFlagEvaluations` class) plus `evaluateFlags()` in `client.ts:1997-2168`, backed by a 740-line dedicated test suite (`__tests__/evaluate-flags.spec.ts`). Verified in detail: (1) `isEnabled`/`getFlag`/`getFlagPayload` (`feature-flag-evaluations.ts:125-157`) only index into a private `_flags` record captured at construction — no `await`, no network call in any accessor; (2) `evaluateFlags()` tries local evaluation first, and only if local coverage is incomplete makes exactly one `getFeatureFlagDetailsStateless(...)` call, merging only keys not already locally resolved (`client.ts:2085-2101`); (3) `_flags` is `private readonly` and never mutated after construction; (4) `getFlagPayload()` never calls the access-tracking hook; (5) `isEnabled`/`getFlag` call `_recordAccess(key)` which forwards to the same `distinctIdHasSentFlagCalls` dedupe tracker (`client.ts:2192-2223`) used by the legacy single-flag path, correctly deduped by key/response/group; (6) `capture()`/`captureImmediate()` accept `flags?: FeatureFlagEvaluations`, and in `_prepareEventMessage` a supplied snapshot always wins over the deprecated `sendFeatureFlags`, even warning the caller if both are passed (`client.ts:2976-2999`), with zero additional evaluation calls; (7) `only(keys)`/`onlyAccessed()` are pure in-memory filters with no network/evaluation, and filtered clones correctly copy (not share) their `_accessed` set so child bookkeeping doesn't leak back to the parent; (8) missing distinct id or a disabled client both return a safe empty snapshot (`distinctId: ''`, `flags: {}`) with a logged warning and no further host calls, and a failed remote fallback (`getFeatureFlagDetailsStateless` swallows fetch failure to `undefined` rather than throwing) simply preserves whatever was already resolved locally. One minor, non-blocking gap: no dedicated regression test simulates the remote `/flags` call literally throwing (as opposed to returning a quota-limited/error-flagged response) while local results already exist — the code path clearly supports it structurally, but this specific scenario lacks direct test pinning.
- This is recorded ✅ given the depth and correctness of the implementation; the untested throw-path is noted for completeness but does not reflect an actual code gap.

### n11 — Group Identify (🟡 Partial)
- **Spec requires:** `both` applicability. Validate that `groupType`/`groupKey` are present and non-empty; drop the call with zero enqueued events and a validation warning otherwise (acceptance scenario "Group identify requires type and key," tagged `@both`).
- **SDK currently:** `groupIdentify()`/`groupIdentifyImmediate()` (`client.ts:2278-2340`) correctly build the `$groupidentify` event with `$group_type`/`$group_key`/`$group_set`, matching the happy-path scenario, including the documented synthetic-distinct-id fallback (`` `$${groupType}_${groupKey}` ``) when `distinctId` is omitted. Fresh read of both methods confirms **there is still no validation** of `groupType`/`groupKey` presence/emptiness — a call with a missing or empty `groupKey` still builds and enqueues a `$groupidentify` event with `$group_key: undefined`, contradicting the acceptance scenario's "no event should be enqueued" requirement. Unchanged from the previous audit.
- **Backwards compatibility:** Needs deprecation path — adding a drop-on-missing-key check changes behavior for any caller currently getting an event enqueued with a falsy/empty type or key; recommend rolling out as a warning first, then dropping in a later minor/major release.
- **Remediation:** Add presence/emptiness validation on `groupType`/`groupKey` in `groupIdentify()`/`groupIdentifyImmediate()`, logging a warning and returning without enqueueing when either is missing/empty.

### n12 — Identify (🟡 Partial)
- **Spec requires:** `both` applicability. `distinct_id` must be present and non-empty; if missing, drop the call silently with no identity event enqueued (acceptance scenario "Identify validates distinct id," tagged `@both`). Spec's new anonymous-to-identified `$set`-transition scenario is explicitly a client-side concept.
- **SDK currently:** `identify()`/`identifyImmediate()` (`client.ts:922-978`) build the correct `$identify` event shape (`$set`/`$set_once`/passthrough `$anon_distinct_id` handling matches spec, with no auto-stamped anonymous id since server SDKs hold no ambient identity). The new anon-to-identified scenario is confirmed tagged `@client` only in `identify.feature` (immediately preceding "Client identify changes the current distinct id..."), with no `@server` counterpart, matching spec.md's explicit statement that server SDKs infer no anonymous id — correctly out of scope for Node, not a regression. **Unchanged defect:** no runtime validation of `distinctId` emptiness — traced the full path `identify()` → `_prepareEventMessage` (`client.ts:2924,2934-2937`): a falsy `distinctId` is silently replaced with a synthesized `uuidv7()` (plus `$process_person_profile: false`) and the event is still enqueued, rather than being dropped with a validation warning. A whitespace-only distinctId (`'   '`) is worse — truthy in JS, so it passes through completely unvalidated as the literal `distinct_id`.
- **Backwards compatibility:** Backward-compatible for an empty/undefined guard; Medium risk if extended to whitespace-only strings, since some callers may rely on the current fallback-UUID behavior (designed for anonymous captures) — recommend logging a warning first before hard-dropping.
- **Remediation:** Add a presence/emptiness (and ideally whitespace-trim) check on `distinctId` in `identify()`/`identifyImmediate()` before calling `_sendPreparedEvent`, logging a validation warning and returning without enqueuing. (Same fix pattern as n4/n11 — recommend addressing all three identity-linking methods together, since they share one root cause in `_prepareEventMessage`.)

### n13 — Is Feature Enabled (❌ Fail)
- **Spec requires:** Hard `SHALL`, `@both`, no SDK carve-out: "The SDK SHALL accept a caller-supplied boolean default ... and SHALL return it whenever the flag has no value" (missing key, not loaded, failed request). A flag with any present value always wins over the default. (This requirement predates the current audit window — it is not newly introduced.)
- **SDK currently:** `isFeatureEnabled(key, distinctId, options?)` (`client.ts:1717-1748`) still has **no `defaultValue` parameter anywhere** in its options object. On a missing/undetermined flag it unconditionally returns `undefined` (line 1741), with no way for a caller to force a `true`/`false` fallback. The designated successor, `FeatureFlagEvaluations.isEnabled(key)` (`feature-flag-evaluations.ts:125-129`), takes only a `key` argument and hardcodes `flag?.enabled ?? false` — a fixed, non-caller-suppliable default. Neither the primary API nor its successor satisfies the spec's hard requirement; confirmed unchanged via fresh reads of both functions and a repo-wide grep for `defaultValue` returning zero hits in either file.
- **Backwards compatibility:** Backward-compatible — adding `defaultValue?: boolean` to `isFeatureEnabled`'s options object and a second optional parameter to `isEnabled()` are both purely additive optional parameters.
- **Remediation:** Add `defaultValue?: boolean` to `isFeatureEnabled()`'s options (or, since it's deprecated, prioritize adding it — and a `sendFeatureFlagEvents`-suppression option — to `evaluateFlags(...).isEnabled()` instead).

### n14 — Register / Unregister (🟡 Partial, both contracts)
- **Spec requires:** Spec's own `## Applicability` says `client`, and the acceptance files (`register.feature`, `unregister.feature`) carry a file-level tag `@public @canonical_behavior @acceptance @register @client` (resp. `@unregister @client`) with no per-scenario override — unambiguously scoping the "persist across restart" acceptance scenario to client SDKs only. Requires: merge properties into a persistent super-properties store; persist to local storage so it survives app restarts; affect future event enrichment unless overridden per-call; unregister removes a single property and is a no-op if the property doesn't exist.
- **SDK currently:** posthog-node genuinely implements both: `register(properties): Promise<void>` and `unregister(property): Promise<void>` on `packages/core/src/posthog-core-stateless.ts:1091-1106`, inherited unmodified by `PostHogBackendClient`. `register` merges into `this.props` and persists via `setPersistedProperty`; `_prepareEventMessage` merges `this.props` into every outgoing event, with per-call properties correctly overriding registered ones. Both are tested and functionally correct for the in-process lifetime of a client, but still neither is part of the documented `IPostHog` public interface (`packages/node/src/types.ts`, confirmed no `register`/`unregister` members exist there) — real-but-undocumented functionality. Node's default storage is still `PostHogMemoryStorage` — pure in-memory, zero disk/fs persistence — so a process restart loses all registered properties. Since the acceptance scenario asserting cross-restart persistence is `@client`-only, Node is technically out of scope for that specific scenario; the residual issue is narrower than a hard compliance failure — it's an API-surface/documentation-hygiene gap (undocumented but reachable methods with surprising in-memory-only semantics for anyone who assumes register/unregister durability from other SDKs' docs).
- **Backwards compatibility:** Backward-compatible — documenting `register`/`unregister` in the public `IPostHog` interface is additive; adding an optional disk-backed storage adapter for cross-restart durability would also be additive (opt-in), not a default-behavior change.
- **Remediation:** Add `register`/`unregister` to the documented `IPostHog` interface in `packages/node/src/types.ts`; if cross-restart durability is desired, document that it requires supplying a custom persistent `PostHogStorage` implementation, since the default in-memory storage cannot satisfy it.

### n15 — Set/Reset Person/Group Properties For Flags (❌ Fail, all four contracts)
- **Spec requires:** Each of these four specs' own `## Applicability` line says `client` ("Server SDKs usually take person/group properties directly on each flag-evaluation call instead of maintaining a persistent local override store"). However, all four acceptance feature files (`set-person-properties-for-flags.feature`, `set-group-properties-for-flags.feature`, `reset-person-properties-for-flags.feature`, `reset-group-properties-for-flags.feature`) are tagged `@both` on the feature line *and on every individual scenario*. Per this audit's acceptance-precedence convention, these are in-scope, not N/A.
- **SDK currently:** `grep -rn "setPersonPropertiesForFlags\|setGroupPropertiesForFlags\|resetPersonPropertiesForFlags\|resetGroupPropertiesForFlags"` across `packages/node/src` and `packages/core/src` still returns zero matches. These four methods exist only on the client-only `PostHogCore` base class, which `PostHogBackendClient` does not extend. Node's only mechanism remains per-call `personProperties`/`groupProperties` kwargs on `getFeatureFlag`/`getAllFlags`/etc. — a fundamentally different, non-persistent mechanism.
- **Backwards compatibility:** Backward-compatible — a new opt-in stateful override store (e.g. instance- or `AsyncLocalStorage`-context-scoped, mirroring the existing tracing-headers context pattern) consulted as a default merged with explicit per-call `personProperties`/`groupProperties`, would be purely additive.
- **Remediation:** Add `setPersonPropertiesForFlags()`/`setGroupPropertiesForFlags()`/`resetPersonPropertiesForFlags()`/`resetGroupPropertiesForFlags()` on the Node `PostHog` client, backed by an instance- or context-scoped override store consulted by all flag-evaluation call sites. Also worth flagging upstream to sdk-specs maintainers: all four specs' `Applicability: client` prose and their acceptance files' `@both` tagging are inconsistent and should be reconciled.

### n16 — Set Person Properties (✅ Pass, beyond stated scope)
- Spec's own `## Applicability` says `client` explicitly, and the acceptance file is `@client`-only with no scenario override. Unlike posthog-python, posthog-node genuinely built a matching method: `setPersonProperties({ distinctId, properties, propertiesOnce })` builds `$set`/`$set_once` and calls `capture({ event: '$set', ... })`, with an explicit empty-guard matching the spec's "Empty person property updates do not crash" requirement precisely. Recorded as ✅ rather than ➖ since the contract is genuinely implemented and functioning.

### n17 — Shutdown (🟡 Partial)
- **Spec requires:** `@both`. Flush pending events, stop workers permanently, reject/no-op future captures, be idempotent, not crash on delivery failure. **New spec-text correction:** Promise-based SDKs SHOULD *resolve* (not reject) on shutdown-timeout, logging a critical diagnostic instead of throwing an unhandled rejection that could crash the host process; reject is still permitted for other unrecoverable flush failures.
- **SDK currently:** The timeout-resolves-not-rejects correction is **already fully satisfied** and was the reason the spec text itself was corrected (per posthog-js#4506) — no code change was needed here. `PostHogCoreStateless._shutdown` races `doShutdown()` against a timeout via `raceWithTimeout` (`packages/core/src/utils/index.ts:97-121`); the timeout branch calls the `onTimeout` callback (which never throws) and then always `resolve()`s, so `Promise.race` settles as a resolution — `shutdown()`'s returned promise resolves on timeout, never rejects. `posthog-core-stateless.ts` logs `this._logger.critical('Timeout while shutting down PostHog. Some events may not have been sent.', {...})` inside that same `onTimeout` callback — `critical` always writes to `console.error` unconditionally, satisfying "logging a critical diagnostic." Genuine unrecoverable errors (non-fetch exceptions in `doShutdown()`'s catch block) are still rethrown, correctly preserving "reject is still permitted for other unrecoverable flush failures." The rest of the contract holds: flush-then-close ordering, idempotency via a `shutdownPromise` guard with a warning log on concurrent re-entry, permanent worker-stop (flush timer cleared, feature-flag poller stopped via `stopPoller()`), the feature-flag-called dedupe tracker explicitly cleared in a `finally` block (`client.ts`, `this.distinctIdHasSentFlagCalls = {}`), and delivery failures during final flush absorbed via `logFlushError()` rather than crashing. **Unchanged gap:** post-shutdown `capture()` calls are still **not** rejected or no-op'd — fresh reads of `wrap()`, `enqueue()`, and `sendImmediate()` in `posthog-core-stateless.ts` confirm no `_isShutdown`/`isShutdown`/`closed` guard exists anywhere in core or node; only `disabled`/`optedOut`/queue-size checks are present. A `capture()` call issued after `shutdown()` resolves silently re-populates the persisted queue and can even re-arm the flush timer that shutdown just cleared, directly contradicting the acceptance scenario "Shutdown flushes queued events and disables future work," which requires that no event be enqueued after shutdown.
- **Backwards compatibility:** Backward-compatible — adding an `_isShutdown` guard checked in `wrap()`/`enqueue()` (mirroring the existing `disabled` check) that silently no-ops future `capture()` calls after shutdown completes is additive; post-shutdown capture calls are already undocumented/unsupported usage.
- **Remediation:** Add a shutdown-state check to `enqueue()`/`sendImmediate()`, alongside the existing `disabled`/`optedOut` checks, that silently drops calls made after `shutdown()` has completed.

### n18 — Before Send Hook (✅ Pass)
- Spec requires `both` applicability: a `beforeSend(event) -> event | null` hook run after event assembly and before enqueue; supports multi-hook chaining, stopping on a drop; hook exceptions must not crash the caller. Node exceeds the baseline: `_beforeSend?: BeforeSendFn | BeforeSendFn[]` (`packages/core/src/posthog-core.ts`) accepts either a single function or an array. `_runBeforeSend()` on `PostHogCore` threads `result = fn(result)` through each hook in order, stops immediately on a falsy/null return, and wraps each hook call in its own try/catch that logs the error and falls back to the last good `result`. Note: this class (`PostHogCore`, used by browser/react-native) is architecturally distinct from the Node-client-specific `_runBeforeSend` referenced in n5, which lacks the same per-hook try/catch — see n5 for that separate deviation in the Node capture path specifically. The `before-send-hook` capability itself, as exercised through the shared core event-assembly path, is fully spec-conformant and tested (`before-send-hook.feature`, `@both`, 3 scenarios).

### n19 — Event Batcher (✅ Pass, with one minor gap)
- **Spec requires:** `both`. Accumulate events; flush at count threshold or interval timer; preserve FIFO order; explicit flush drains immediately; optionally ("some SDKs") shrink the batch and retry on HTTP 413.
- **SDK currently:** Defaults `flushAt=20`/`maxBatchSize=100`/`flushInterval=10000ms` in core, with Node overriding `maxQueueSize=10000`/`flushInterval=5000ms`. Count-threshold and interval-timer flushing both correctly implemented. FIFO batch slicing with multi-batch drain per flush call verified. 413 adaptive batch-size shrink-and-retry is implemented for the v0 pipeline (`posthog-core-stateless.ts:1604-1615`, halves `maxBatchSize` and retries without persisting the queue removal) — but **v1's sender still has no equivalent**: `RETRYABLE_STATUSES = new Set([408, 500, 502, 503, 504])` (`capture-v1/sender.ts:20`) omits 413 entirely, so it's immediately terminal in v1 with no shrink-and-retry. Since the spec explicitly frames 413-shrink-retry as optional ("some SDKs"), this asymmetry between v0 (default) and v1 (opt-in) is a minor deviation, not a hard violation — scored Pass overall.
- **Backwards compatibility:** Backward-compatible — adding 413-shrink-and-retry to `V1CaptureSender` mirroring the existing v0 behavior is additive.
- **Remediation:** Low priority given the spec frames this as optional; if pursued, port the v0 413-shrink-and-retry logic to `capture-v1/sender.ts`.

### n20 — Exception Event Metadata (❌ Fail)
- **Spec requires:** `both` applicability. Canonical `$exception_list` envelope with a `mechanism` object carrying `type`/`handled`/`source`/`synthetic`/`exception_id`/`parent_id`; a capture-category vocabulary and `$exception_source` naming convention (e.g. `node.process_uncaught_exception`, `node.process_unhandled_rejection`); explicit handled/synthetic state that is never silently defaulted to `false` (or any value) when genuinely unknown — it must be omitted; nested-exception tree linkage via `exception_id`/`parent_id` with a 50-entry truncation cap and cycle detection; `$exception_level` normalization (including `fatal` for terminating boundaries); typed-override vs. property-bag precedence (application property bags must never override SDK-owned `$exception_*` keys); native debug-image metadata (expected N/A for Node, which has no native address frames); a producer/processor property-ownership table.
- **SDK currently:** Deep read of `packages/core/src/error-tracking/error-properties-builder.ts`, `types.ts`, all coercers, `parsers/index.ts`, and `packages/node/src/extensions/error-tracking/{index.ts,autocapture.ts}` found multiple hard failures:
  - **`mechanism.exception_id`/`parent_id` do not exist at all** — the `Mechanism` type (`packages/core/src/error-tracking/types.ts:46-51`) has only `handled`/`type`/`source`/`synthetic`. No tree-linkage identifiers are ever assigned anywhere (confirmed via exhaustive grep, zero hits in non-spec code). Consequently there is no cycle detection either — the only depth guard is an unrelated `MAX_CAUSE_RECURSION = 4` (`error-properties-builder.ts:20,141`), far short of, and structurally unrelated to, the spec's "at most 50 entries, cycle-detected, depth-first" requirement. (A separate `STACKTRACE_FRAME_LIMIT = 50` in `parsers/index.ts:41` caps stack *frames within one exception*, not the exception *list* length — the two must not be conflated in remediation.)
  - **`$exception_source` is entirely unimplemented** — spec requires values like `node.process_uncaught_exception`/`node.process_unhandled_rejection`; grep across the whole codebase returns zero hits for `exception_source`.
  - **Nested `.cause` entries hardcode `handled: true`** regardless of true state (`convertToExceptionList`, `error-properties-builder.ts:119-126`, comment: "Cause errors are necessarily handled") — directly violating "Each nested entry SHALL preserve its own independently known handled state... MUST NOT copy, invert, or otherwise derive the value solely from the outermost entry." Also `mechanism.handled ?? true` (line ~108) silently defaults unknown to `true` rather than omitting it — the spec requires omission for genuinely unknown state, not any particular default.
  - **`mechanism.source` is never set anywhere**, and nested causes inherit the *same* `mechanism.type` as the outermost exception (spread via `{...mechanism, handled: true}`) instead of the required `chained` — so nested-exception relationship metadata is effectively absent. No `AggregateError`/member-vs-cause distinction exists either (zero hits for `AggregateError`).
  - **`$exception_level` is hardcoded to `'error'`** for every capture path (`error-properties-builder.ts:42`) — manual capture, uncaught exceptions, and unhandled rejections all get the same literal, even though Node's default `uncaughtException` handling terminates the process (`autocapture.ts:106-110`, `process.exit(1)`), which the spec's table says should map to `fatal`. `SeverityLevel`/`severityLevels` types exist and are parsed by `ObjectCoercer.isSeverityLevel`, but the parsed value is never actually read by the builder — dead code.
  - **Property-bag precedence is inverted**: `ErrorTracking.buildEventMessage` (`packages/node/src/extensions/error-tracking/index.ts:52,67-69`) spreads caller-supplied `additionalProperties` *after* the SDK-canonical `exceptionProperties`, so a caller passing `$exception_list`/`$exception_level` in their properties silently overrides the SDK's own canonical values — directly violating "Application property bags MUST NOT override SDK-owned `$exception_*` properties."
  - **Native debug-image metadata is correctly N/A** — confirmed no `$debug_images`/`debug_id`/`image_addr` usage anywhere except an unused, never-populated `StackFrame.instruction_addr?: string` type field; this matches the spec's explicit carve-out for SDKs without native address-based frames.
  - **`$exception_fingerprint`** has no typed override API at all — compliant with "must not invent one" (there's no path to invent), but doesn't implement the positive requirement to accept and preserve a caller-supplied override.
- Given the number and severity of these failures (missing tree-linkage fields entirely, incorrect handled-state derivation, hardcoded severity, and an inverted override-precedence bug that lets application code corrupt SDK-owned metadata), this is scored ❌ Fail rather than Partial.
- **Backwards compatibility:** Needs deprecation path for `handled`/`$exception_level` value changes (current shipped payloads would change shape for existing consumers); Backward-compatible for adding `exception_id`/`parent_id`/`$exception_source`/reserved-key protection (all additive/hardening).
- **Remediation:** Add `exception_id`/`parent_id` generation and 50-entry truncation with cycle detection to `convertToExceptionList`; add `$exception_source` derivation from the capture site (autocapture hooks vs. manual `captureException`); thread real per-cause `handled` state instead of hardcoding `true`, and omit (not default) genuinely unknown state; set `mechanism.source`/`type: 'chained'` correctly for nested causes; derive `$exception_level` from mechanism/fatality context (`fatal` for terminating uncaught-exception boundaries) instead of a hardcoded literal; reserve `$exception_*` keys in `ErrorTracking.buildEventMessage` so caller-supplied `additionalProperties` cannot override them.

### n21 — Feature Flag Called Tracker (🟡 Partial)
- **Spec requires:** Dedup `$feature_flag_called` by `(flag, value, distinct_id, normalized groups)`; bounded/incremental eviction (capacity pressure SHOULD NOT clear the entire tracker); clear on shutdown; minimal-event mode gated by `has_experiment===false`, with an allowlist including 10 session-attribution properties while still excluding `$referrer`.
- **SDK currently — allowlist sub-item remains fully compliant:** the canonical allowlist lives in shared `packages/core/src/featureFlagUtils.ts` (`MINIMAL_FLAG_CALLED_EVENT_CAMPAIGN_PROPERTIES` + `MINIMAL_FLAG_CALLED_EVENT_PROPERTIES`), confirmed unchanged: all 10 required session-attribution properties present, `$referrer` explicitly excluded, plus 14 additional click-id parameters beyond spec. `packages/node/src/client.ts` imports and calls `minimizeFlagCalledEventProperties` directly from `@posthog/core` — the same constant `packages/browser` consumes, so node inherits this for free.
- **Gap — capacity eviction is still a full clear, not incremental:** fresh read confirms `client.ts:2207-2208` still does `if (Object.keys(this.distinctIdHasSentFlagCalls).length >= this.maxCacheSize) { this.distinctIdHasSentFlagCalls = {} }` (`MAX_CACHE_SIZE = 50_000`). This directly violates the spec's requirement that capacity pressure SHOULD NOT clear the entire tracker — at scale, hitting the cap floods analytics with a burst of duplicate `$feature_flag_called` events for every active user simultaneously. (The separate full clear inside `_shutdown()` is correct, since shutdown is an explicit lifecycle boundary, not capacity pressure.) Dedup key construction and shutdown-time clearing both remain correct.
- **Backwards compatibility:** Backward-compatible — swapping the full-clear for an LRU/bounded map with incremental eviction changes no public API.
- **Remediation:** Replace `distinctIdHasSentFlagCalls`'s capacity handling with incremental LRU eviction (e.g. a `SizeLimitedDict`-style structure, as posthog-python already uses for the same purpose).

### n22 — Flag Definition Loader (🟡 Partial)
- **Spec requires:** Loader fetches and caches flag/cohort/group-type-mapping definitions for local evaluation; on a refresh failure, stale definitions are retained (not discarded) and a refresh-failure warning is recorded, without disrupting in-flight local evaluation.
- **SDK currently:** `_loadFeatureFlags()` (`packages/node/src/extensions/feature-flags/feature-flags.ts:883-976`) correctly retains stale definitions on failure — the `default:` branch of its response-status `switch` (lines 968-971) does a bare `return` without calling `updateFlagState`, so existing cached flags are never overwritten by a failed refresh. **However, this default branch also never records any refresh-failure diagnostic** — no `onError?.()` call, no `console.warn`/`this._logger`/`this.onError` invocation of any kind — for any status code other than 401/403/429 (which route through a thrown `ClientError` caught and forwarded to `onError` at lines 973-976). A 503 (or 500, or any other non-explicitly-handled status) — a realistic and common transient failure mode — silently falls through with zero observability, directly failing the acceptance scenario "Loader keeps stale definitions when refresh fails," which explicitly requires that "the SDK should record a flag definition refresh warning" in addition to retaining stale data. This is a newly-identified gap not flagged in the previous audit (which rated this row ✅).
- **Backwards compatibility:** Backward-compatible — adding a warning log call to an already-silent failure branch has no API impact.
- **Remediation:** In the `default:` branch of `_loadFeatureFlags()`'s response-status switch, add a `this._logger.warn(...)` (or `this.onError?.(...)`) call recording the unexpected status code before returning, so refresh failures are observable without changing the stale-data-retention behavior.

### n23 — HTTP Client (🟡 Partial)
- **Spec requires:** Native HTTP client injectable/replaceable; ingestion requests include token; gzip; non-2xx/3xx failures reported without throwing to capture callers; a feature-flag-evaluation retry policy: retry only on transport failures or HTTP 502/504, default budget of exactly 1 retry (2 total attempts), exponential backoff starting at 300ms and doubling to 600ms for the second retry.
- **SDK currently:** Native `fetch` (injectable `FetchLike`) used throughout, no axios/undici dependency. Ingestion requests correctly include the token and gzip via native `zlib.gzip`. Non-2xx/3xx failures are reported without throwing to `capture()` callers. The `/flags` retry classification remains correct (`isRetryableFlagsFetchError`: network errors except `ECONNREFUSED`, or 502/504 only) and the default retry budget is correct (`featureFlagsRequestMaxRetries` defaults to `1`, giving 2 total attempts). **The backoff timing is still wrong**: fresh read of `retriable()` (`packages/core/src/utils/index.ts:51-78`) confirms it still uses one fixed `props.retryDelay` (default 3000ms) for every retry iteration — no exponential logic exists anywhere in `packages/core`. The `/flags` call site passes no `retryDelay` override, so it inherits the fixed 3000ms default rather than the spec-mandated 300ms→600ms exponential sequence, failing the timing-sensitive acceptance scenarios in `http-client.feature`.
- **Backwards compatibility:** Backward-compatible — changing the fixed 3000ms delay to the spec's exponential 300ms/600ms schedule for the flags-retry path specifically is an internal timing change with no public API impact (aside from tests asserting the old timing).
- **Remediation:** Introduce a dedicated exponential-backoff schedule (300ms base, doubling, capped at the 1-retry default) for the `/flags` retry path in `retriable()`/`isRetryableFlagsFetchError`'s call site, distinct from the general-purpose fixed-delay retry used elsewhere.

### n24 — Local Feature Flag Evaluator (🟡 Partial)
- **Spec requires:** Property-matcher dispatch for all standard operators; rollout-percentage hashing; multivariate bucketing; cohort/nested-cohort matching; person/group property override merging; group-type flag evaluation with correct remote-fallback deferral. **New requirement:** unrecognized property-filter operators must degrade to inconclusive for *only* the affected flag (never raise/crash), without blocking other flags in the same local-evaluation batch pass.
- **SDK currently:** The general matching engine (property-matcher dispatch, SHA1-based rollout hashing matching the canonical `sha1("{key}.{distinct_id}{salt}")`/`LONG_SCALE` algorithm, multivariate bucketing, nested/negated cohort matching, person/group property override merging) was independently re-verified against a full read of `packages/node/src/extensions/feature-flags/feature-flags.ts` and remains correct — no changes since the previous audit. **The new unrecognized-operator requirement is fully satisfied:** `matchProperty`'s operator switch (lines 1090-1251) has a `default:` case that throws a purpose-built `InconclusiveMatchError` (not a generic crash), which is caught per-condition-group in `matchFeatureFlagProperties` (lines 501-598) and re-thrown as a fresh `InconclusiveMatchError` if nothing matched conclusively. Critically, batch isolation is correct: `getAllFlagsAndPayloads` (lines 209-255) evaluates each flag inside its own try/catch nested in a `Promise.all(...)` map, so an `InconclusiveMatchError` thrown for one flag cannot reject the overall promise or block any other flag's evaluation — each flag's catch block is local to that flag's own promise. This exactly matches the spec's new scenarios. One minor process gap (not a code gap): the acceptance `.feature` file (`local-feature-flag-evaluator.feature`) still only contains the 4 original scenarios — the new operator-degradation scenarios exist in `spec.md` but have not yet been mirrored into the `.feature` test asset, a spec/test-asset sync gap worth flagging upstream, though the code itself already satisfies the requirement. Overall rated 🟡 rather than ✅ purely because of this asset-sync gap and to keep this row distinguishable for tracking — the code itself has no defect found.
- **Backwards compatibility:** N/A — no code change needed for the operator-degradation requirement itself.
- **Remediation:** Add the two new operator-degradation scenarios from `spec.md` into `acceptance/private/local-feature-flag-evaluator.feature` so this compliance status can be verified by the shared test harness going forward, not just by manual code audit.

### n25 — Retry Queue (🟡 Partial)
- **Spec requires:** `both`. In-memory queue with backpressure/drop-when-full; classify retryable (network errors, 5xx, 429, timeouts) vs. non-retryable (most 4xx) failures; exponential/linear backoff honoring `Retry-After`; retryable failures keep the same events queued for a later, separate flush attempt; success removal by identity/stable-id (not index); terminal/exhausted failures drop the batch.
- **SDK currently:** Retryable classification (v0): network errors, 408/429, or 5xx; non-retryable 4xx correctly drops with an emitted/rethrown error. Overflow handling is oldest-dropped FIFO eviction with a warning log. Post-flush removal is identity-based (matched by UUID first, falling back to object-reference identity), correctly satisfying the anti-regression requirement about concurrently-captured events not being mistaken for the just-sent batch. `Retry-After` is honored, but **only in v1** (`capture-v1/sender.ts:130,165,300-326`) — v0 (still the default pipeline) never reads `Retry-After` anywhere, confirmed unchanged by fresh grep. v0 uses fixed-delay (not exponential) retry — spec-permitted ("exponential *or linear*"), so not a violation, just the weaker of the two policies; v1 does true exponential backoff. The same v1-events-can-be-lost-on-exhausted-retry issue documented under Flush (n9) applies here too — directly contradicting "retryable failures keep the same events queued for a later attempt" for the v1 pipeline specifically, though currently latent since v1 is opt-in-only.
- **Backwards compatibility:** Backward-compatible to add `Retry-After` support to v0 (additive). The v1 queue-loss issue needs the same careful, non-silent fix described under n9.
- **Remediation:** Add `Retry-After` honoring to the v0 retry path in `packages/core/src/utils/index.ts`'s `retriable()`; fix the v1 event-loss issue per n9's remediation.
