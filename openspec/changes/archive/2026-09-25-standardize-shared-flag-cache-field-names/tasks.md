## 1. Standardize the shared payload contract

- [x] Require endpoint-aligned snake_case payload field names while preserving language-specific provider APIs.
- [x] Add a cross-SDK shared-payload scenario.
- [x] Validate the change with `openspec validate standardize-shared-flag-cache-field-names --strict`.
